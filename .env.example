# Klleon Chat SDK key from Klleon Studio (SDK Management).
# Prefer KLLEON_SDK_KEY in Docker/production (read at runtime).
# NEXT_PUBLIC_KLLEON_SDK_KEY still works for local `next dev`.
KLLEON_SDK_KEY=
NEXT_PUBLIC_KLLEON_SDK_KEY=
PORT=8081
